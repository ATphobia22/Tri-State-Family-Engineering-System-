# Canonical Archimedes engine / LOMA-BCA path (CI hard gate)
fastapi==0.111.0
uvicorn==0.30.1
reportlab==4.2.0
pydantic==2.7.4
httpx==0.27.0
python-multipart==0.0.31

# Optional hydrology clients (not required for CI; install for richer NWIS / GW)
# dataretrieval @ https://github.com/ATphobia22/dataretrieval-python  (or PyPI: dataretrieval)
# flopy  # for MODFLOW 6 coupling when groundwater seepage is enabled
